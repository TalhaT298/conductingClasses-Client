import React from 'react';
import { Link } from 'react-router-dom';

const Teacher = () => {
    return (
        <div>
           <Link to="/teacher">
           
           </Link> 
        </div>
    );
};

export default Teacher;